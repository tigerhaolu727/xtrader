# Validation Plan (XTR-015)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-015` —— 校验 Spec/Validation 资产完整性（守门检查）。
- [x] 单元测试回归（开发后执行）—— 运行 `tests/unit/backtests`，确保离线查看器加载与渲染链路不回归。
- [ ] 目录入口验收（开发后执行）—— 验证控制区仅保留 Run 目录选择；移除独立 `run_manifest.json` 选择控件。
- [ ] 目录一致性验收（开发后执行）—— 验证仅可加载所选目录内 manifest/artifacts；缺失或冲突时显示明确中文报错。
- [ ] 布局可读性验收（开发后执行）—— 验证页面顺序为 `Kline -> Equity -> Timeline -> Trade Ledger`，并确认不再采用 Timeline/Ledger 左右分栏。
- [ ] 中文文案验收（开发后执行）—— 验证按钮、状态提示、表头与主要 Tooltip 中文一致，关键标题支持“中文（English）”双语格式且术语统一。
- [ ] 交互入口验收（开发后执行）—— 验证“窗口选择器快捷定位 + 图内缩放/scrollbar 精细控制”可同时工作，且二者切换无冲突。
- [ ] LOD 行为验收（开发后执行）—— `ALL` 初始为聚合 K 线，连续放大后粒度自动细化，足够放大后回到原始 `5m`。
- [ ] 回退行为验收（开发后执行）—— 从细粒度视窗缩小回大视窗时，K 线自动回退为聚合展示。
- [ ] 标注联动验收（开发后执行）—— 缩放过程中交易标注始终跟随当前可视窗口刷新，不出现“集中左侧”问题。
- [ ] 轴范围验收（开发后执行）—— Y 轴随当前展示序列动态重算，缩放前后范围合理。
- [ ] 账本联动验收（开发后执行）—— 点击 `Trade Ledger` 行时，`Kline` 与 `Equity` 同步定位到对应时间窗口，`Timeline` 自动切换到 Trade Focus。
- [ ] Timeline 模式验收（开发后执行）—— 验证 `Global/Trade Focus` 切换可用，Trade Focus 仅展示目标交易相关信号执行数据，且可返回 Global。

## Execution Log
- 2026-03-30 19:10 CST
  - 变更：创建任务 `XTR-015`，生成 Spec/Validation 模板。
  - 命令：`python scripts/task_guard.py new XTR-015 --title "离线报告UI按可视窗口动态K线粒度"`
  - 结果：通过（已生成 `docs/03-delivery/specs/XTR-015.md` 与 `docs/03-delivery/validation/XTR-015.md`）。
- 2026-03-30 19:10 CST
  - 变更：根据评审结论补齐需求描述（动态 LOD、缩放重算、raw/agg 切换、标注/轴联动）。
  - 状态：待实现阶段落地代码并执行后续验证项。
- 2026-03-30 19:10 CST
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（Spec/Validation 资产合规）。
- 2026-03-30 19:17 CST
  - 变更：根据评审结论更新交互要求，明确“图内缩放/scrollbar 为主交互，窗口选择器保留为快捷入口”，并补充对应验证项。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（文档更新后守门检查合规）。
- 2026-03-30 19:24 CST
  - 变更：根据评审结论补充布局优化要求，明确 `Equity` 移至 `Kline` 正下方，并新增账本联动 `Kline/Equity` 同步定位验收项。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（文档更新后守门检查合规）。
- 2026-03-30 19:37 CST
  - 变更：根据评审结论更新页面主布局为 `Kline -> Equity -> Timeline -> Trade Ledger`，并为 Timeline 增加 `Global/Trade Focus` 模式与账本点击触发聚焦要求。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（文档更新后守门检查合规）。
- 2026-03-30 19:37 CST
  - 变更：根据评审结论补充“仅 Run 目录加载（移除独立 manifest 选择）”与“页面文案统一中文”要求，并新增对应验证项。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（文档更新后守门检查合规）。
- 2026-03-30 19:46 CST
  - 变更：根据评审结论更新文案策略，明确“中文为主 + 关键标题可采用中文（English）双语样式”，并同步调整文案验收口径。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（文档更新后守门检查合规）。
- 2026-03-30 20:00 CST
  - 变更：完成 `XTR-015` 实现，范围包含：
    - 离线查看器移除独立 `run_manifest.json` 选择入口，仅保留 Run 目录加载并增加多 manifest 冲突检测。
    - 页面主布局改为纵向：`Kline -> Equity -> Timeline -> Trade Ledger`。
    - `Timeline` 新增 `Global/Trade Focus` 模式，并支持账本点击触发聚焦联动。
    - K 线缩放时按当前可视窗口动态重算 raw/agg 粒度（LOD），并同步刷新标注与 Y 轴。
    - 文案调整为中文优先，关键标题采用“中文（English）”。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_offline_viewer.py tests/unit/backtests/test_event_driven.py`
  - 结果：通过（`13 passed`）。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests`
  - 结果：通过（`16 passed`）。
  - 命令：`python -m py_compile src/xtrader/backtests/offline_viewer.py scripts/offline_report_viewer.py`
  - 结果：通过。
  - 命令：`python scripts/offline_report_viewer.py init`
  - 结果：通过（viewer 目录资源已同步到最新实现）。
  - 命令：`python scripts/task_guard.py check XTR-015`
  - 结果：通过（实现后守门检查合规）。

## Evidence
- Spec: `docs/03-delivery/specs/XTR-015.md`
- Validation: `docs/03-delivery/validation/XTR-015.md`
