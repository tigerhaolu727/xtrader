# Validation Plan (XTR-014)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-014` —— 校验 Spec/Validation 资产完整性（守门检查）。
- [x] 产物结构验收（开发后执行）—— 运行一次策略回测，确认不再生成 CSV 产物，仅保留 Parquet 对应文件。
- [x] Manifest/索引验收（开发后执行）—— 校验 `run_manifest.json` 与 report 索引路径均指向 Parquet，不再包含 CSV 路径。
- [ ] UI 功能验收（开发后执行）—— 在“仅 Parquet”报告目录中验证 K线、交易、资金曲线、Timeline 均可加载展示。
- [ ] UI 回归验收（开发后执行）—— 校验 `5m/15m/1h/4h/1d` 时间粒度展示、窗口内 OHLC 桶聚合、交易 entry/exit 标注、账本点击联动缩放、Timeline 聚合/明细切换均不退化。
- [ ] 数据一致性验收（开发后执行）—— 对比改造前基线 run 的关键指标，确认统计结果一致。
- [ ] 异常场景验收（开发后执行）—— 人工构造缺失/损坏 Parquet，确认 UI 给出明确错误提示。

## Execution Log
- 2026-03-30 16:46 CST
  - 变更：创建任务 `XTR-014`，补齐 Spec 与 Validation 初稿。
  - 命令：`python scripts/task_guard.py check XTR-014`
  - 结果：通过（Spec/Validation 资产合规）。
  - 状态：待用户确认后进入开发实现阶段。
- 2026-03-30 16:47 CST
  - 命令：`python scripts/task_guard.py check XTR-014`
  - 结果：通过（文档复核，资产持续合规）。
- 2026-03-30 16:52 CST
  - 变更：根据评审问题补充“UI 不回归”约束，更新 Spec 的 Requirement/Design/Acceptance 与 Validation 回归验收项。
  - 状态：待实现阶段按新增回归项执行验证。
- 2026-03-30 16:53 CST
  - 命令：`python scripts/task_guard.py check XTR-014`
  - 结果：通过（更新后文档守门检查合规）。
- 2026-03-30 17:10 CST
  - 变更：完成 XTR-014 实现，范围包括：
    - 回测输出移除 CSV（trades/equity/timeline/snapshot），统一 Parquet。
    - chunk 文件格式由 CSV 切换为 Parquet。
    - 离线 UI 接入 `hyparquet`（vendor 到 `assets/vendor/hyparquet`），改为 Parquet-only 读取。
    - report index 路径切换为 parquet。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py tests/unit/backtests/test_offline_viewer.py`
  - 结果：通过（`13 passed`）。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests`
  - 结果：通过（`16 passed`）。
  - 命令：`python -m py_compile src/xtrader/backtests/event_driven.py src/xtrader/backtests/offline_viewer.py scripts/offline_report_viewer.py scripts/report_server.py`
  - 结果：通过。
  - 命令：`python scripts/task_guard.py check XTR-014`
  - 结果：通过（实现后守门检查合规）。
  - 备注：UI 功能/回归项尚待人工浏览器验收。
- 2026-03-30 17:27 CST
  - 变更：为人工 UI 验收准备真实三年样本 run（`BTCUSDT`、`5m`、`ThresholdIntradayStrategy`）。
  - 命令：`PYTHONPATH=src python - <<'PY' ... write_strategy_event_driven_outputs(..., run_suffix=\"btcusdt_5m_3y_ui_validation_v2\")`
  - 结果：
    - 产出目录：`reports/backtests/strategy/threshold_intraday/20260330T092657Z_threshold_intraday_btcusdt_5m_3y_ui_validation_v2/`
    - `price_rows=314094`，`actions_rows=314094`，`trade_count=6332`
    - `resampled_timeframes=15m,1h,4h,1d`
    - 目录内未发现 `*.csv`，产物为 Parquet-only（含 chunk parquet）
  - 用途：作为后续 UI 功能/回归/异常场景人工验收输入数据。

## Evidence
- Spec: `docs/03-delivery/specs/XTR-014.md`
- Validation: `docs/03-delivery/validation/XTR-014.md`
