# Validation Plan (XTR-013)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-013` —— 校验 Spec/Validation 资产完整性（守门检查）。
- [x] 结构验收（开发后执行）—— 运行一次策略回测，确认不再生成旧报告文件，仅产出标准数据文件。
- [x] Manifest 验收（开发后执行）—— 校验 `run_manifest.json` 能唯一索引并加载所有产物，路径/rows/hash 字段完整。
- [x] 重采样快照验收（开发后执行）—— 在 15m/1H/1D 策略样例中，验证 `snapshots/base/price_5m.parquet`、`snapshots/resampled/price_<tf>.parquet`、`snapshots/resampled/resampled_manifest.json` 完整落盘，且重采样规则与来源可追溯。
- [ ] UI 验收（开发后执行）—— 通过单一 manifest 入口展示 K线、交易记录、资金曲线、Timeline，并验证 UTC 同轴联动与 K 线动态 Y 轴。
- [ ] 大数据可视化验收（开发后执行）—— 使用“5m 三年（>30 万 K线）”样例验证视窗优先加载、分块读取、降采样、三图联动与页面可响应性。
- [ ] Trade Ledger 性能验收（开发后执行）—— 在上万交易记录下验证虚拟滚动/分页生效，页面无一次性渲染卡死。
- [ ] Timeline 聚合验收（开发后执行）—— 大时间窗使用聚合展示，小时间窗自动切换明细连线展示，hover 明细准确。

## Execution Log
- 2026-03-30 14:22 CST
  - 命令：`python scripts/task_guard.py check XTR-013`
  - 结果：通过（Spec/Validation 资产合规）。
- 2026-03-30 14:39 CST
  - 变更：补充大数据 UI 优化条款（视窗优先、分块、降采样、联动、表格虚拟化）到 Spec/Validation。
  - 状态：文档更新完成，待实现后执行对应验证。
- 2026-03-30 14:52 CST
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：通过（`11 passed`）；验证回测输出已切换为纯数据产物（新增 `run_manifest/diagnostics/signal_execution`，移除 `event_driven_report.*` 与 `report_hub.*` 断言），并覆盖 5m 基础快照 + 15m/1h 重采样快照落盘。
- 2026-03-30 14:52 CST
  - 命令：`python scripts/task_guard.py check XTR-013`
  - 结果：通过（Spec/Validation 资产合规）。
- 2026-03-30 15:07 CST
  - 变更：补齐离线查看器初始化能力。
    - 新增模块：`src/xtrader/backtests/offline_viewer.py`（复制 `offline_report_viewer.html` + `echarts.min.js`）。
    - 新增脚本：`scripts/offline_report_viewer.py`（`init` 命令，一键初始化离线查看器目录）。
    - 新增测试：`tests/unit/backtests/test_offline_viewer.py`。
- 2026-03-30 15:07 CST
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py tests/unit/backtests/test_offline_viewer.py`
  - 结果：通过（`13 passed`）。
- 2026-03-30 15:07 CST
  - 命令：`python -m py_compile src/xtrader/backtests/event_driven.py src/xtrader/backtests/offline_viewer.py scripts/offline_report_viewer.py`
  - 结果：通过。
- 2026-03-30 15:07 CST
  - 命令：`python scripts/task_guard.py check XTR-013`
  - 结果：通过（Spec/Validation 资产合规）。
- 2026-03-30 15:08 CST
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests`
  - 结果：通过（`16 passed`）。
- 2026-03-30 15:30 CST
  - 变更：继续完善离线 UI 大数据加载链路（修复 `withinWindow` 未定义、接通 `chunk_sets + chunk manifest` 读取、默认窗口按时间窗增量加载、Timeline 大窗聚合/小窗明细切换）。
  - 影响文件：`src/xtrader/backtests/assets/offline_report_viewer.html`、`tests/unit/backtests/test_offline_viewer.py`。
- 2026-03-30 15:31 CST
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_offline_viewer.py tests/unit/backtests/test_event_driven.py`
  - 结果：通过（`13 passed`）。
- 2026-03-30 15:31 CST
  - 命令：`python scripts/task_guard.py check XTR-013`
  - 结果：通过（Spec/Validation 资产合规）。
- 其余开发期验证待执行。

## Evidence
- Spec: `docs/03-delivery/specs/XTR-013.md`
- Validation: `docs/03-delivery/validation/XTR-013.md`
