# Validation Plan (XTR-016)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-016` —— 校验 Spec/Validation 资产完整性（守门检查）。
- [ ] 缩放可逆性人工验收（开发后执行）—— 在 K 线上连续 zoom in 后执行 zoom out，确认可回到更大时间范围与全局视图。
- [ ] LOD 连续性验收（开发后执行）—— 验证 zoom in/zoom out 过程中仍按可视窗口动态切换 raw/agg，不出现固定粒度或异常跳变。
- [ ] 联动回归验收（开发后执行）—— 验证交易标注与 Y 轴动态范围在缩放过程中持续联动。
- [x] 自动化回归（开发后执行）—— 运行 `tests/unit/backtests`，确保离线查看器与回测相关单测通过。

## Execution Log
- 2026-03-30 20:11 CST
  - 变更：创建任务 `XTR-016`，生成 Spec/Validation 模板。
  - 命令：`python scripts/task_guard.py new XTR-016 --title "离线K线缩放后无法缩小的视窗域修复"`
  - 结果：通过（已生成 `docs/03-delivery/specs/XTR-016.md` 与 `docs/03-delivery/validation/XTR-016.md`）。
- 2026-03-30 20:11 CST
  - 变更：根据问题复现与代码分析补齐需求描述，明确根因为“局部 series 更新导致可缩放域被持续收缩”，并给出修复方向（固定 x 轴全局域 + 保留视窗重采样）。
  - 状态：待实现阶段编码与验证。
- 2026-03-30 20:11 CST
  - 命令：`python scripts/task_guard.py check XTR-016`
  - 结果：通过（Spec/Validation 资产合规）。
- 2026-03-30 20:14 CST
  - 变更：完成 `XTR-016` 代码修复，关键点：
    - K 线 x 轴显式固定为 `_klineSource` 全局时间边界（`xAxis.min/max`），避免 series 子集持续收缩可缩放域。
    - 视窗重采样保持按当前可视范围执行，保留 LOD 能力。
    - `visible.length === 0` 时不回退全量重绘，改为保持当前图层并仅更新 x 轴域，降低缩放抖动风险。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_offline_viewer.py tests/unit/backtests/test_event_driven.py`
  - 结果：通过（`13 passed`）。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests`
  - 结果：通过（`16 passed`）。
  - 命令：`python -m py_compile src/xtrader/backtests/offline_viewer.py scripts/offline_report_viewer.py`
  - 结果：通过。
  - 命令：`python scripts/offline_report_viewer.py init`
  - 结果：通过（viewer 目录资源已同步到最新实现）。
  - 命令：`python scripts/task_guard.py check XTR-016`
  - 结果：通过（实现后守门检查合规）。

## Evidence
- Spec: `docs/03-delivery/specs/XTR-016.md`
- Validation: `docs/03-delivery/validation/XTR-016.md`
