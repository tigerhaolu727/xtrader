# Validation Plan (XTR-001)

## Planned Validation
- [ ] `pytest tests/exchanges/test_bitget_client.py::test_balances` —— mock REST 响应，验证账户余额解析。
- [ ] `pytest tests/exchanges/test_bitget_client.py::test_positions` —— mock REST 响应，验证持仓解析。
- [ ] `pytest tests/exchanges/test_bitget_client.py::test_fetch_klines` —— mock REST 响应，验证历史 K 线分页与模型映射。
- [ ] `python examples/bitget_client_demo.py` —— 实际或模拟环境下运行演示脚本，展示 API 配置和调用流程（仅 REST 功能）。
- [ ] `PYTHONPATH=src pytest tests/integration/test_bitget_client_live.py` —— 当环境设置 Bitget 凭证时，验证真实 API URL/auth 是否可用（无凭证自动跳过）。
- [ ] `PYTHONPATH=src pytest tests/integration/test_proxy_connectivity.py` —— 验证 `BITGET_HTTP_PROXY/BITGET_HTTPS_PROXY` 所指代理端口是否可建立 TCP 连接。

## Execution Log
- 2026-03-26: `PYTHONPATH=src pytest tests/unit/exchanges/test_bitget_client.py` —— 5 tests passed，覆盖账户、持仓、K 线和行情元数据解析。
- 2026-03-26: `PYTHONPATH=src pytest tests/integration/test_bitget_client_live.py` —— 环境未配置凭证，测试被自动跳过。
- 2026-03-26: `PYTHONPATH=src pytest tests/integration/test_bitget_client_live.py`（配置代理后）—— 因无法连接到本地代理 `127.0.0.1:9090` 被权限限制，抛出 `httpx.ConnectError`；待在可访问代理的环境中重新运行。
- 2026-03-26: `PYTHONPATH=src pytest tests/integration/test_proxy_connectivity.py` —— 因沙箱禁止连接本地代理端口导致失败（同上）；待在允许访问代理的环境重试。
- 2026-03-26: `PYTHONPATH=src pytest tests/integration/test_bitget_client_live.py -vv` —— 依旧无法连接代理 `127.0.0.1:7890`，触发 `httpx.ConnectError: [Errno 1] Operation not permitted`；需在可用代理或直连网络的环境重试。

## Evidence
- 参见本地 pytest 输出；CI 尚未接入。
- 集成测试需在具备 `BITGET_API_*` 凭证并允许访问代理/外网的环境中执行以获取通过结果。
- 代理连通性测试同样需要在代理可访问的环境中运行。
