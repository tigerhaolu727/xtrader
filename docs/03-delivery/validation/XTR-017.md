# Validation Plan (XTR-017)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-017` —— 守门检查，确保 Spec/Validation 资产完整。
- [ ] 单元测试回归（开发后执行）—— 覆盖离线 UI 主源切换状态机、Auto/Manual 模式转换、旧 run 兜底逻辑。
- [ ] 交互验收（开发后执行）—— 验证 `Auto` 默认生效且执行周期优先、执行周期缺失时按“邻近周期优先”回退、手动切换与切回 Auto 行为正确。
- [ ] 联动回归验收（开发后执行）—— 验证主源切换后 dataZoom、Y 轴、交易标注、Ledger/Timeline/Equity 联动不退化。
- [ ] 多周期样例验收（开发后执行）—— 在同时含 `5m/15m/1h/4h/1d` 的 run 中验证可用周期识别、切换与 `snapshot 懒加载` 缓存行为。
- [ ] 兼容性验收（开发后执行）—— 使用不含 `kline_sources` 的旧 run 验证降级路径可用且提示清晰。
- [ ] 范围约束验收（开发后执行）—— 确认 v1 未引入多周期 chunk 读取分支，避免超范围实现。

## Execution Log
- 2026-03-31 10:12 CST
  - 变更：创建任务 `XTR-017`，生成 Spec 与 Validation 模板。
  - 命令：`python scripts/task_guard.py new XTR-017 --title "离线报告UI支持多K线主源手动切换"`
  - 结果：通过（模板生成成功）。
- 2026-03-31 10:14 CST
  - 变更：根据用户确认方案补齐 Spec/Validation 初稿，关键口径：
    - 默认模式 `Auto`
    - `Auto` 规则“执行周期优先”
  - 状态：待用户评审后进入实现阶段。
- 2026-03-31 10:15 CST
  - 命令：`python scripts/task_guard.py check XTR-017`
  - 结果：通过（文档守门检查合规）。
- 2026-03-31 10:22 CST
  - 变更：根据讨论结论更新范围，明确 `v1= snapshot 懒加载`，`chunk 化=后续版本`。
  - 影响：同步更新 Spec 的 Requirement/Design/Acceptance，以及 Validation 的多周期验收口径与范围约束验收项。
- 2026-03-31 10:23 CST
  - 命令：`python scripts/task_guard.py check XTR-017`
  - 结果：通过（更新后文档守门检查合规）。
- 2026-03-31 10:32 CST
  - 变更：根据讨论结论更新 Auto 回退口径为“邻近周期优先”。
  - 影响：
    - Spec 中将固定回退顺序替换为按 `interval_ms` 距离排序的邻近回退规则。
    - 补充等距决策规则，降低实现歧义。
    - Validation 交互验收项同步改为验证邻近回退逻辑。
- 2026-03-31 10:33 CST
  - 命令：`python scripts/task_guard.py check XTR-017`
  - 结果：通过（更新后文档守门检查合规）。

## Evidence
- Spec: `docs/03-delivery/specs/XTR-017.md`
- Validation: `docs/03-delivery/validation/XTR-017.md`
