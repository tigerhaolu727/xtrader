# Validation Plan (XTR-011)

## Planned Validation
- [x] `python scripts/task_guard.py check XTR-011` —— 验证 Spec/Validation 资产完整性。
- [x] 代码清理检查 —— 删除选币/截面相关模块与测试（策略、回测、因子截面评估）。
- [x] 因子栈清理检查 —— 删除 `xtrader.factors` 与 `FeatureStore`，不保留旧因子适配层。
- [x] 导出与接口检查 —— `strategies` 与 `backtests` 主路径仅保留 CTA 交易闭环所需模块。
- [x] 回归测试 —— 验证 data/strategies/backtests/exchanges 单测通过。
- [x] 可视化复盘检查 —— `event_driven_report.html` 包含时间轴面板与单笔交易回放筛选器。
- [x] 回放交互检查 —— 支持 `Window Bars` 聚焦与 hover 交易详情卡片。
- [x] 可视化可读性检查 —— Entry 标注不遮挡 K 线；时间轴提供含义说明与 hover 交互详情。
- [x] 长周期报告性能检查 —— 3 年 5m 数据下报告可打开并保持可交互（无浏览器卡死）。
- [x] Trade Ledger 分页/虚拟化检查 —— 默认仅渲染当前页，翻页与筛选可用。
- [x] 分片按需加载检查 —— `manifest + chunks` 输出存在，时间范围切换可自动加载对应分片。
- [x] 时间范围联动检查 —— K线/时间轴/权益/账本在同一时间窗下同步更新。
- [x] 长周期视图优化检查 —— 聚合视图与单笔放大视图均可用。
- [x] 本地静态服务器模式检查 —— `http://localhost` 访问可自动加载分片；`file://` 场景有清晰提示。
- [x] 报告总览页检查 —— `report_hub.html` 自动收录历史与新报告，支持自动刷新与快速跳转。
- [x] 静态服务管理命令检查 —— 支持 `start/stop/status/restart`，并可在对话中调用。

## Execution Log
- 2026-03-29
  - 命令：基于用户确认整理 Phase D 需求（1/2/3/4 + 本地静态服务器模式），更新 Spec/Validation 待执行项。
  - 结果：
    - 已更新：`docs/03-delivery/specs/XTR-011.md`（新增长周期报告优化需求、设计与验收口径）
    - 已更新：`docs/03-delivery/validation/XTR-011.md`（新增长周期性能与按需加载验证清单，待执行）
  - 命令：优化 HTML 可视化可读性与时间轴交互（Entry 遮挡修复 + timeline hover details）。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - K 线区：Entry 区域从整块半透明遮罩改为细虚线连接，不再覆盖蜡烛主体。
    - 时间轴区：新增含义说明（Signal/Execution/lag），并支持 hover 显示 action/reason/signal_time/execution_time/lag。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`6 passed`
  - 命令：`PYTHONPATH=src python <demo script>` 生成可视化样例报告。
  - 结果：产物 `reports/backtests/XTR-011/20260329T135454Z_demo_trades_v2/event_driven_report.html` 可用。
  - 命令：继续增强 HTML 回放交互（K 线窗口聚焦 + hover 交易详情卡片）。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - `Replay Trade` 模式新增 `Window Bars` 控件（前后 N 根 K 线窗口聚焦）
    - 新增 `tradeDetails` 卡片（显示 side/time/price/qty/cost/net/exit_reason）
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`6 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`48 passed`
  - 命令：`python scripts/task_guard.py check XTR-011`
  - 结果：`✓ Spec & Validation check passed.`
  - 命令：推进 R4（聚合视图 + Trade Spotlight）与 file 模式复制命令按钮。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - 新增：`Kline Mode`（`Range` / `Trade Spotlight`）
    - 新增：`file://` 提示区 `Copy Command`（复制 `python -m http.server 8000`）
    - 新增：自动 K 线聚合（5m/1h/4h/1d 自动选取，目标 bars<=1200）
    - 新增：K 线交易标注上限（`_KLINE_TRADE_MARKER_MAX=1500`）避免长周期 SVG 过大
    - 新增：`Overview TF` 标签显示当前聚合周期
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`7 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`49 passed`
  - 命令：`PYTHONPATH=src python <real 3y script>` 生成 R4 版本报告。
  - 结果：
    - 产物：`reports/backtests/XTR-011/20260329T150148Z_real3y_chunked_v5/`
    - `trade_count=65660`，`manifest.total_trades=65660`，`chunk_count=37`
    - `event_driven_report.html=6,417,817 bytes`
    - 相比 `v4`（`39,073,673 bytes`）再下降 `83.58%`
  - 命令：实现报告总览页（hub）并接入自动注册。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - `write_event_driven_outputs` 现在自动写入：
      - `report_hub_index.json`
      - `report_hub.html`
    - 新增历史报告自动发现（扫描已有 `summary.json + event_driven_report.html` 目录），避免只显示新报告。
    - 新增 Hub 自动刷新（默认 5s）、搜索过滤、直达 report/summary/trades 链接。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`9 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`51 passed`
  - 命令：`PYTHONPATH=src python <hub bootstrap script>`
  - 结果：
    - `reports/backtests/XTR-011/report_hub.html` 已生成
    - `reports/backtests/XTR-011/report_hub_index.json` 已生成
    - 当前收录 `report_count=10`
  - 命令：新增静态服务管理脚本与 README 指南。
  - 结果：
    - 已新增：`scripts/report_server.py`（`start/stop/status/restart`，含 PID 状态与日志）
    - 已更新：`README.md`（Report Hub Static Server 用法）
    - `python -m py_compile scripts/report_server.py` 通过
    - 本环境中绑定端口受限（`PermissionError`），脚本已输出可读错误并保留日志路径
  - 命令：修复 Kline 首屏可见性（默认 Range 改为 ALL）并重生成报告。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`、`tests/unit/backtests/test_event_driven.py`
    - `PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py` -> `9 passed`
    - `PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py` -> `51 passed`
    - 新报告：`reports/backtests/XTR-011/20260329T154607Z_real3y_chunked_v6/event_driven_report.html`
  - 命令：实现 R3 范围联动（Range 同步 K线/Timeline/Equity/Trade Ledger）。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - 新增全局时间边界计算 `_build_dashboard_time_bounds`
    - `timelineSvg/equitySvg` 增加 `id + data-*` 元信息，前端 `viewBox` 按 Range 同步缩放
    - 非 chunked 账本行增加 `data-entry-ms`，可随 Range 过滤
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`7 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`49 passed`
  - 命令：`PYTHONPATH=src python <real 3y script>` 生成联动版本报告。
  - 结果：
    - 初版 `reports/backtests/XTR-011/20260329T144711Z_real3y_chunked_v3/` 因交易标注属性冗余导致 HTML 增大到 `58,443,373 bytes`
    - 已移除冗余属性并重跑，最终产物：`reports/backtests/XTR-011/20260329T145008Z_real3y_chunked_v4/`
    - `trade_count=65660`，`manifest.total_trades=65660`，`chunk_count=37`
    - 最终 `event_driven_report.html=39,073,673 bytes`
  - 命令：`PYTHONPATH=src python <real 3y script>`（真实 BTCUSDT 5m 数据，近 3 年，含 fee/slippage）
  - 结果：
    - 产物：`reports/backtests/XTR-011/20260329T142915Z_real3y_chunked/`
    - `bars_3y=315649`，`trades=65660`
    - `event_driven_report.html=38,947,679 bytes`（较此前约 89MB 显著下降）
    - 生成 `trade_ledger_manifest.json` + `chunks/trades_*.json`（`chunk_count=37`，总分片大小 `24,328,516 bytes`）
    - HTML 含 `file://` 模式提示、range 控件、ledger 分页控件与 chunk 自动加载脚本入口
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`7 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`49 passed`
  - 命令：补充 manifest 统计字段（`total_trades`）并更新单测断言。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - 已更新：`tests/unit/backtests/test_event_driven.py`
  - 命令：补齐 Range 预设 `7D`（与 Spec R3 一致）。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - `PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py` -> `7 passed`
    - `PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py` -> `49 passed`
  - 命令：重新生成 3 年 chunked 报告（包含 `7D` 预设与 `total_trades`）。
  - 结果：
    - 产物：`reports/backtests/XTR-011/20260329T143602Z_real3y_chunked_v2/`
    - `trade_count=65660`，`manifest.total_trades=65660`
    - `event_driven_report.html=38,947,783 bytes`，`chunk_count=37`
  - 命令：增强事件回测 HTML 仪表盘（多周期信号/执行时间轴 + 单笔交易回放筛选器）。
  - 结果：
    - 已更新：`src/xtrader/backtests/event_driven.py`
    - `write_event_driven_outputs` 新增可选参数：`actions`、`signal_interval_ms`
    - `event_driven_report.html` 新增：
      - `Signal vs Execution Timeline` 面板（signal_tf vs exec_tf）
      - `Replay Trade` 下拉筛选器（按 `data-trade-id` 高亮单笔）
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`6 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`48 passed`
  - 命令：按“BTC 自动交易主线”清理选币相关模块。
  - 结果：
    - 已删除：`src/xtrader/strategies/legacy.py`
    - 已删除：`src/xtrader/strategies/portfolio.py`
    - 已删除：`src/xtrader/strategies/builtin_strategies/`
    - 已删除：`src/xtrader/backtests/walk_forward.py`
    - 已删除测试：`tests/unit/strategies/test_portfolio.py`、`tests/unit/strategies/test_legacy_namespace.py`、`tests/unit/backtests/test_walk_forward.py`
  - 命令：继续按用户确认移除原因子研发链路与存储链路。
  - 结果：
    - 已删除：`src/xtrader/factors/`（全部模块，包括 `FeatureStore`、评估 CLI、因子实现）
    - 已删除：`src/xtrader/backtests/factor_backtest.py`
    - 已删除：`tests/unit/factors/`
    - 已删除：`tests/unit/backtests/test_factor_backtest.py`
    - 已更新：`src/xtrader/backtests/__init__.py`（移除 factor backtest 导出）
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`48 passed`
  - 命令：按用户要求创建策略实现子目录 `src/xtrader/strategies/builtin_strategies/`，并将 `ThresholdIntradayStrategy` 实现迁入 `threshold_intraday.py`。
  - 结果：`intraday.py` 调整为兼容导出层，`strategies/__init__.py` 与 `strategies/builtin.py` 已改为从新目录导入，后续新增策略可统一目录管理。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/strategies tests/unit/backtests`
  - 结果：`28 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`48 passed`
  - 命令：增强事件回测报告输出，新增 `event_driven_report.html`（K线图+入场/出场标注+权益曲线+逐笔交易表），保留原有 `summary.json/trades.csv/equity_curve.csv/event_driven_report.md`。
  - 结果：`write_event_driven_outputs` 支持可选 `price_frame` 输入并输出 HTML 仪表盘，便于可视化复盘。
  - 命令：`PYTHONPATH=src pytest -q tests/unit/backtests/test_event_driven.py`
  - 结果：`6 passed`
  - 命令：`PYTHONPATH=src pytest -q tests/unit/data tests/unit/strategies tests/unit/backtests tests/unit/exchanges/test_bitget_client.py`
  - 结果：`48 passed`
  - 命令：`python scripts/task_guard.py check XTR-011`
  - 结果：`✓ Spec & Validation check passed.`

## Evidence
- 文档：`docs/03-delivery/specs/XTR-011.md`
- 文档：`docs/03-delivery/validation/XTR-011.md`
- 代码：`src/xtrader/strategies/__init__.py`
- 代码：`src/xtrader/strategies/builtin.py`
- 代码：`src/xtrader/strategies/builtin_strategies/__init__.py`
- 代码：`src/xtrader/strategies/builtin_strategies/threshold_intraday.py`
- 代码：`src/xtrader/strategies/intraday.py`
- 代码：`src/xtrader/strategies/state_machine.py`
- 代码：`src/xtrader/strategies/risk.py`
- 代码：`src/xtrader/backtests/__init__.py`
- 代码：`src/xtrader/backtests/event_driven.py`
- 代码：`src/xtrader/backtests/leakage_guard.py`
- 测试：`tests/unit/strategies/test_base.py`
- 测试：`tests/unit/strategies/test_builtin.py`
- 测试：`tests/unit/strategies/test_intraday.py`
- 测试：`tests/unit/strategies/test_state_machine.py`
- 测试：`tests/unit/strategies/test_risk.py`
- 测试：`tests/unit/backtests/test_event_driven.py`
- 测试：`tests/unit/backtests/test_leakage_guard.py`
