# Validation Plan (XTR-002)

## Planned Validation
- [x] `python - <<PY ...` 打印 `BITGET_HTTP_PROXY/BITGET_HTTPS_PROXY` 变量 —— 23:14:04（本地时间）显示两个代理变量均未设置。
- [x] `python -m httpx https://api.bitget.com/api/spot/v1/public/time` —— 23:14:09 请求失败，抛出 `ConnectError('[Errno 8] nodename nor servname provided, or not known')`。
- [x] `curl https://ifconfig.me` —— 紧接着的 `curl -v` 返回 `Could not resolve host: ifconfig.me`。

## Execution Log
- 2026-03-26 23:14:04 CST：执行 `python - <<PY ...` 打印代理变量，结果均为 `<unset>`。
- 2026-03-26 23:14:09 CST：执行 `python - <<PY ... httpx.get(bitget time)`，DNS 解析失败，HTTP 未发出。
- 2026-03-26 23:14:10 CST：执行 `curl -v https://ifconfig.me`，同样因 `Could not resolve host` 终止，证明当前沙箱无法解析公网域名。

## Evidence
- 见上述命令输出（终端日志）；已在本地控制台保留。
